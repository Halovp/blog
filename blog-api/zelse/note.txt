一、博客列表主页·
1.建立maven工程，坐标
2.文章列表开发/articles  post
    service没有加注解
    写完一个部分就要检测运行一次；
    mapper的包扫描的时候路径不正确
    postman传参在body里raw-json格式或者x-www-form-urllencoded
3.首页文章列表实现，不知道是做什么的
    mp无法实现多表联查
    getnickname()方法
    ****在resource设计mapper.xml文件时，目录用的是点（.）分隔符,一直报无效绑定的错误
    ****应该用（/）
4.设计最热标签，还是不太熟悉项目总体结构
5.设置统一异常处理
6.最热文章，不明白参数是什么，要返回什么
7.文章归档，根据时间整理显示,即按照年月分类，用SQL语句时发现时间数据有问题，格式不对，查询后
显示为null，%144719
二、登陆注册jwt
    1.登录：## 1.2 JWT
       登录使用JWT技术。
       *****把登陆归为另外一个功能*******注入的又是同一个对象
       jwt 可以生成 一个加密的token，做为用户登录的令牌，当用户登录成功之后，发放给客户端。
       请求需要登录的资源或者接口的时候，将token携带，后端验证token是否合法。
       jwt 有三部分组成：A.B.C
       A：Header，{"type":"JWT","alg":"HS256"} 固定 //头部信息
       B：playload，存放信息，比如，用户id，过期时间等等，可以被解密，不能存放敏感信息  //负载
       C:  签证，A和B加上秘钥 加密而成，只要秘钥不丢失，可以认为是安全的。 //
       jwt 验证，主要就是验证C部分 是否合法。
       ********加密盐加密*********
    建立error对象（枚举类），方便错误码的返回，解耦合
    2.获取用户信息  token前端获取到之后，会存储在 storage中 h5 ，本地存储
        由于不会redis,故redis验证部分暂时无法通过,写完后发现加载不出头像，检查SQL代码发现设置出错
        并且登录并获取用户信息的请求方式应该改为get
        退出登录，清除redis中的信息
    3.注册功能（后期加一个验证功能） 搞不明白为什么在写注册功能mapper对象之类的时要加this,
    @Transaction添加事务，保证注册不出问题，
    将token添加到redis中时字母拼写错误，
    mybatisplus中不设置主键自增的话默认使用的雪花算法，有利于分布式

    4.登录拦截器的实现，在mvc中注册使 拦截器生效，
    //如果不删除Threadlocal中用完的用户信息，会有内存泄露的风险
         每一个Thread维护一个ThreadLocalMap, key为使用**弱引用**的ThreadLocal实例，value为线程变量的副本。

         引用**，使用最普遍的引用，一个对象具有强引用，不会被垃圾回收器回收。当内存空间不足，Java虚拟机宁愿抛出OutOfMemoryError错误，使程序异常终止，也不回收这种对象。

         如果想取消强引用和某个对象之间的关联，可以显式地将引用赋值为null，这样可以使JVM在合适的时间就会回收该对象。**

         弱引用**，JVM进行垃圾回收时，无论内存是否充足，都会回收被弱引用关联的对象。在java中，用java.lang.ref.WeakReference类来表示。
三、文章内容，restful风格，@PathVariable("“）表明路径参数
    1.使用重载实现原有的方法实现，不改变原来的方法（重载实现方法复用）
    ***********
        有个问题：前端没有渲染出标签categroryvo数据出来；
        2.使用线程池实现阅读数的增加,但感觉阅读数是实时增加的，好快，不知道加的线程干了什么
        3.基本数据类型会被初始化，一定要用封装类类型，阅读次数bug功能修正
    4.评论列表及盖楼；盖两层
    分布式id默认生成，故事用bigint
          //防止前端 精度损失 把id转为string
        // 分布式id 比较长，传到前端 会有精度损失，必须转为string类型 进行传输，就不会有问题了
            @JsonSerialize(using = ToStringSerializer.class)
            private Long id;
    5.写文章功能，这卡了我一会
    6.AOP记录日志：靠日志看出很多问题，日志代码不能切入代码
        手写注解，引用了两个工具类
    7.文章分类，感觉copy方法还是要单独提出来,根据标签分类查询文章，同一路径下，有不同的参数，有点问题
    8.标签分类，标签分类查文章
    9.文章归档
    10.在上传图片的时候，由于工具类默认华北地区，空间存储仓库在华南，古一直无法上传成功
    到此博客功能基本完成，
四，缓存优化
    1.内存的访问速度 远远大于 磁盘的访问速度 （1000倍起）aop，感觉缓存的优化我做不来

    1. 文章可以放入es当中，便于后续中文分词搜索。springboot教程有和es的整合
    2. 评论数据，可以考虑放入mongodb当中  电商系统当中 评论数据放入mongo中
    3. 阅读数和评论数 ，考虑把阅读数和评论数 增加的时候 放入redis incr自增，使用定时任务 定时把数据固话到数据库当中
    4. 为了加快访问速度，部署的时候，可以把图片，js，css等放入七牛云存储中，加快网站访问速度

5.做一个后台 用springsecurity 做一个权限系统，对工作帮助比较大

总体写完了，但感觉不是我自己写而出来的
回头再来看吧



